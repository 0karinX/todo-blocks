/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TodoFormFillerDirective } from './todo-form-filler.directive';

describe('TodoFormFillerDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoFormFillerDirective();
    expect(directive).toBeTruthy();
  });
});
